import tw, { styled } from "twin.macro";

const Button = styled.button(() => ({
  ...tw`bg-[#495464] rounded-[14px] w-full h-[55px] text-2xl text-[#E8E8E8] font-normal leading-none`,
}));

export default Button;
